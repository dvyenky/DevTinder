# DevTinder

app.use("/endPoint",(req, res) => {
// these is known as route handler
})

http methods in RestAPI
get, post, put, patch, delete

always connect first Database then connect to server
because if there any issue occured so it will get to catch the database

mostly all the method inside the mongoose whether its fetch or insert or update or delete it will always return the promise

what is the id in mongoDB and what is the \_\_v in document/collection
